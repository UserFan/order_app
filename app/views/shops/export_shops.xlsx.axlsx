wb = xlsx_package.workbook
wb.add_worksheet(name: "Shop") do |sheet|
  @shops.each do |shop|
    sheet.add_row [shop.name,
                   button.address,
                   shop.type.name,
                   shop.user.full_name,
                   shop.user.phone,
                   # shop.email,
                   # shop.type.name,
                   # shop.type.name,
                   # shop.type.name,
                   # shop.type.name,
                   shop.email]
  end
end
